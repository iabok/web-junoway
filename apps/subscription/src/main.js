require(['jquery', 'select2', 'underscore', 'backbone', 
	'datepicker',
	'date-ui',
	'backbone-validation',
	'stickit',
	'bootstrap',
	'flexslider',
	'intlTelInput',
	 'app'], 
	function ($, select2, _ ,Backbone, datepicker, dateUi, BackboneValidation, Stickit, Bootstrap, Flexslider, intlTelInput, Router) {
    var router = new Router();
    Backbone.history.start();
});